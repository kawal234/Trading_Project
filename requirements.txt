# Core HTTP client
requests>=2.31.0

# CLI framework
typer[all]>=0.12.0

# Rich terminal output (pulled in by typer[all], listed explicitly for clarity)
rich>=13.7.0

# Type hints backport (Python < 3.10)
typing-extensions>=4.9.0
